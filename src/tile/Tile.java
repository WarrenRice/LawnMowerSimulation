package tile;

import java.awt.image.BufferedImage;

public class Tile {
	
	public BufferedImage image;			// Define image as BufferedImage
	public boolean boundary = false;	// Define and instantiate boundary as a boolean set to false (boundary)
	public boolean perimeter = false;	// Define and instantiate perimeter as a boolean set to false
	public boolean dock = false;		// Define and instantiate dock as a boolean set to false

}

